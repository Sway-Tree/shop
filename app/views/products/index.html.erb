<h1>Listing Products</h1>
 
<%= link_to 'New product', new_category_product_path %>

<table>
  <tr>
    <th>Item</th>
    <th>Description</th>
    <th>Price</th>
  </tr>
 
  <% @products.each do |product| %>
    <tr>
      <td><%= product.item %></td>
       <td><%= product.description %></td>
        <td><%= product.price %></td>
          <td><%= link_to 'Show', category_product_path(@category, product) %></td>
      <td><%= link_to 'Edit', edit_category_product_path(@category, product)   %></td>
      <td><%= link_to 'Destroy', category_products_path ,
                    method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>