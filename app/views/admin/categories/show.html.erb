<%= link_to 'New product', new_admin_category_product_path(@category) %>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<h2>Products</h2>
<%= render @category.products %>

<%= link_to 'Products in details', admin_category_products_path(@category) %>

<p>
<%= link_to 'Edit', edit_admin_category_path(@category) %> | <%= link_to 'Back', admin_categories_path %>
</p>